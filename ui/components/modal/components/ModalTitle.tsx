import { createSimpleComponent } from '@kittu/ui-kit.core.create-simple-component';
import { AnyTag } from '@kittu/ui-kit.core.types';

export const ModalTitle: AnyTag = createSimpleComponent({
  Component: 'h2',
  displayName: 'ModalTitle',
});
